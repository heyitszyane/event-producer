# Pinned dependencies as of P7A (2026-06-30).
# Re-pin before production deploy: pip freeze > requirements.txt
# P7A: added google-genai for the server-side live-Gemini provider seam.
# The SDK is imported lazily and only in live mode; fallback/test mode does
# not require it.
pydantic==2.13.4
pydantic-core==2.46.4
fastapi==0.138.0
starlette==1.3.1
uvicorn[standard]==0.49.0
anyio==4.14.0
httpx==0.28.1
mypy==2.1.0
mypy-extensions==1.1.0
ruff==0.15.18
pytest==9.1.1
google-genai==2.10.0
google-cloud-firestore==2.21.0
# P7O: agent skill-card frontmatter parsing (previously transitive via uvicorn[standard]).
PyYAML==6.0.3
types-PyYAML==6.0.12.20260518
