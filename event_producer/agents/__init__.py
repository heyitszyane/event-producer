"""Agent package for Event Producer.

This package contains all agent modules for the multi-agent system.
Agent classes are exported here as they are implemented.

Modules:
    orchestrator          - Top-level coordinator that routes messages
    brief_scope           - Brief parsing and scope configuration (reason + formatter)
    budget_manager        - Budget reconciliation agent (reason + formatter)
    production_manager    - Run-of-show scheduling agent (reason + formatter)
    vendor_coordinator    - Vendor communication agent (reason + formatter)
    risk_flagger          - Risk and gap detection agent
"""

# Exports will be added as agents are implemented.
